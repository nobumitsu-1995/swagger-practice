openapi: "3.0.3"

info:
  title: "Sample API"
  version: "1.0.0"

paths:
  "/message":
    get:
      summary: "sample api get operation"
      description: "sample api get operation"
      responses:
        "200":
          description: "success operation"
          content:
            application/json:
              schema:
                type: string
                example: "Hello world!!"

components:
  schemas:
    SampleInt32:
      type: integer
      format: int32
    SampleDateString:
      type: string
      format: date